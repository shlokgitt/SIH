# QR Certification Module — owned by: [ Shriyam]

Build the QR scan and certificate display page here.

## What to build
- QR scan page (use `html5-qrcode` npm package)
- Certificate detail view (pulls from `GET /api/batches/:id/certificate`)
- Compliant / Non-compliant visual badge

## API reference
See `backend/README.md` → Batches section.

## Rules
- Only edit files inside this folder.